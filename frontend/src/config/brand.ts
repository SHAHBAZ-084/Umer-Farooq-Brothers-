export const APP_BRAND_NAME = 'Umer Farooq & Brothers';

/** Public URL for the app logo (served from frontend/public). */
export const APP_LOGO_SRC = '/app-logo.png';

/** Matches `shared/backup-constants.cjs` — keep in sync for UI examples. */
export const BACKUP_FILENAME_PREFIX = 'umer-farooq-backup';
