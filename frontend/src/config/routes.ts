/** App landing (blank) — login, Close, and catch-all land here. */
export const APP_HOME_PATH = '/';

/** Dashboard (PosHomePage) — only via TopBar logo. */
export const APP_DASHBOARD_PATH = '/dashboard';
