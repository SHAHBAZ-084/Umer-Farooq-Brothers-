'use strict';

/**
 * Single source of truth for local + Google Drive backup filename prefixes.
 * Imported by backend (Google Drive backups) and Electron (local backups).
 */
module.exports = {
  BACKUP_FILENAME_PREFIX: 'umer-farooq-backup',
};
